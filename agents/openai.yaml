interface:
  display_name: "Explore"
  short_description: "Delegate codebase reconnaissance to explorer subagents"
  default_prompt: "Use $explore to delegate explorer subagents to map this codebase before making changes."
